# CPP book usage notes

Setup that's needed:

```
pip install -U sphinx
pip install -U spinx-autobuild
pip install -U sphinx_rtd_theme
```


Run ```serve``` bash script to run server.  Scripts directory is on the path (see Dockerfile).